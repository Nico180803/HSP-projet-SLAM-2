<form method="post" id="delete_user_{{ user.id }}_form" action="{{ path('app_user_delete', {'id': user.id}) }}" onsubmit="return confirm('Êtes vous sûr de votre suppression ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
</form>
