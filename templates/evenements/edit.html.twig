{% extends 'base.html.twig' %}

{% block title %}Modifier un événement{% endblock %}

{% block body %}
    <h1>Modifier un événement</h1>

    {% for message in app.flashes('success') %}
        <p>{{ message }}</p>
    {% endfor %}

    {{ include('evenements/_form.html.twig', {'button_label': 'Enregistrer'}) }}

    <a href="{{ path('app_evenements_index') }}">Retour à la liste</a>
{% endblock %}
    
