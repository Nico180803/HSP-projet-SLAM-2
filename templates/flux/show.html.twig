{% extends 'base.html.twig' %}

{% block title %}Flux{% endblock %}

{% block body %}
    <h1>Flux</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ flux.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ flux.titre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_flux_index') }}">back to list</a>

    <a href="{{ path('app_flux_edit', {'id': flux.id}) }}">edit</a>

    {{ include('flux/_delete_form.html.twig') }}
{% endblock %}
