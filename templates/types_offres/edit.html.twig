{% extends 'base.html.twig' %}

{% block title %}Modifier un type d'offre{% endblock %}

{% block body %}
    {{ parent() }}

    <section class="section py-5">
        <div class="container" data-aos="fade-up" data-aos-delay="100">

            <div class="d-flex align-items-center justify-content-between mb-4">
                <div class="section-title m-0">
                    <h2 class="mb-1">Modifier le type d’offre</h2>
                    <p class="text-muted mb-0">Mettez à jour les informations</p>
                </div>
                <div class="d-inline-flex align-items-center gap-2">
                    <a href="{{ path('app_types_offres_index') }}" class="btn btn-outline-secondary">
                        <i class="bi bi-arrow-left me-1"></i> Retour à la liste
                    </a>
                    {{ include('types_offres/_delete_form.html.twig', { types_offre: types_offre }) }}
                </div>
            </div>

            <div class="card shadow-sm border-0" data-aos="fade-up" data-aos-delay="150">
                <div class="card-body p-4">
                    {{ include('types_offres/_form.html.twig', { button_label: 'Mettre à jour' }) }}
                </div>
            </div>

        </div>
    </section>
{% endblock %}
