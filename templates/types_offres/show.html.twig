{% extends 'base.html.twig' %}

{% block title %}TypesOffres{% endblock %}

{% block body %}
    <h1>TypesOffres</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ types_offre.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ types_offre.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_types_offres_index') }}">back to list</a>

    <a href="{{ path('app_types_offres_edit', {'id': types_offre.id}) }}">edit</a>

    {{ include('types_offres/_delete_form.html.twig') }}
{% endblock %}
