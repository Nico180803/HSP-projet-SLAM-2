<form method="post" id="delete_sujets_{{ sujet.id }}_form" action="{{ path('app_sujets_delete', {'id': sujet.id}) }}" onsubmit="return confirm('Êtes vous sûr de votre suppression ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sujet.id) }}">
</form>
