{% extends 'base.html.twig' %}

{% block title %}ContactsEntreprise{% endblock %}

{% block body %}
    <h1>ContactsEntreprise</h1>

    <table class="table">
        <tbody>

            <tr>
                <th>Fonction</th>
                <td>{{ contacts_entreprise.fonction }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ contacts_entreprise.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ contacts_entreprise.prenom }}</td>
            </tr>
            <tr>
                <th>Tel</th>
                <td>{{ contacts_entreprise.tel }}</td>
            </tr>
            <tr>
                <th>Mail</th>
                <td>{{ contacts_entreprise.mail }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_contacts_entreprise_index') }}">back to list</a>

    <a href="{{ path('app_contacts_entreprise_edit', {'id': contacts_entreprise.id}) }}">edit</a>

    {{ include('contacts_entreprise/_delete_form.html.twig') }}
{% endblock %}
